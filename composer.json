{
	"name": "multitest",
	"require": {
		"monolog/monolog": "~1.8"
	},
	"require-dev": {
		"heroku/heroku-buildpack-php": "*"
	},
	"scripts": {
		"post-install-cmd": [
			"$(npm bin -q)/bower install"
		],
		"post-update-cmd": [
			"$(npm bin -q)/bower install"
		]
	}
}